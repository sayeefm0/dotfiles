name: Bootstrap Develop Environment

on:
  push:
  schedule:
    - cron: '0 12 * * *'
    
jobs:
  bootstrap-dev-environment:
    runs-on: macos-12
    steps:
      - uses: actions/checkout@v3
      - run: |
          bash -vx ./bash/bootstrap.sh
