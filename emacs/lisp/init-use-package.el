;;; init-use-package.el --- install use-package with straight
;;; Commentary:
;;; Code:

(straight-use-package 'use-package)

;; TODO: straight-use-package-by-default breaks rustic for some reason
;; figure out what's wrong and then enable straight by default

(provide 'init-use-package)

;;; init-use-package.el ends here
